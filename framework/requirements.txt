# PRIDE Metadata Extraction Framework
# Python dependencies

# LLM Client
openai>=1.0
anthropic>=0.25.0  # required when provider=anthropic (Claude models)

# Embeddings and ML
transformers>=4.30.0
torch>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Progress bars
tqdm>=4.65.0

# HTTP downloads
requests>=2.28.0

# Configuration
PyYAML>=6.0

# Optional: faster nearest neighbor search
faiss-cpu>=1.7.4  # GPU support unavailable on PyPI for this env
# annoy>=1.17.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# DocETL integration
docetl>=0.2.6
pyrate-limiter>=3.9,<4.0  # pinned for docetl compatibility

# Three-step post-extraction QC (negation detector)
spacy>=3.7
negspacy>=1.0.4
